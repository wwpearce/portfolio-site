export const addListener = (elm, func) => {
  let element = document.querySelector(elm);
  element.addEventListener('click', func, false);
};

export const addListeners = (array, func) => {
  let element = document.querySelector(elm);
  element.addEventListener('click', func, false);
};

export const slideIn = (slide) => {
  
};
